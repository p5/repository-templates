{
  "name": "serverless-typescript",
  "version": "1.0.0",
  "description": "Serverless aws-nodejs-typescript template",
  "scripts": {
    "sls:invoke": "yarn sls invoke local --function healthcheck",
    "sls:package": "yarn sls package",
    "sls:deploy": "yarn sls deploy"
  },
  "engines": {
    "node": ">=18.17.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.124",
    "esbuild": "0.19.12",
    "serverless": "3.38.0",
    "serverless-esbuild": "1.50.2",
    "typescript": "5.3.3"
  }
}
